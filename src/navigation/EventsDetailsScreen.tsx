import * as React from "react";
import { EventsDetails } from "@containers";

export default function EventsDetailsScreen({ navigation, route }) {
  return <EventsDetails navigation={navigation} route={route} />;
}
