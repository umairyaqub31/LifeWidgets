import * as React from 'react';
import {TopLeadboards} from '@containers';


export default function TopLeadboardsScreen({navigation}) {
  return (
      <TopLeadboards navigation={navigation}/>
  );
}
