import * as React from 'react';
import {EventsCalendar} from '@containers';


export default function EventsCalendarScreen({navigation}) {
  return (
      <EventsCalendar navigation={navigation}/>
  );
}
