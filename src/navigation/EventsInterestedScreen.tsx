import * as React from "react";
import { EventsInterested } from "@containers";

export default function EventsInterestedScreen({ navigation }) {
  return <EventsInterested navigation={navigation} />;
}
