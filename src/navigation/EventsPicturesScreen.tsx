import * as React from "react";
import { EventsPictures } from "@containers";

export default function EventsPicturesScreen({ navigation, route }) {
  return <EventsPictures navigation={navigation} route={route} />;
}
