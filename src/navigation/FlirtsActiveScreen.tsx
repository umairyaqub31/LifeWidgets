import * as React from 'react';
import { ActiveFlirts } from '@containers';


export default function FlirtsActiveScreen({navigation}) {
  return (
      <ActiveFlirts navigation={navigation}/>
  );
}
