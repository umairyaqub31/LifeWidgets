import * as React from 'react';
import { RequestFlirts } from '@containers';


export default function FlirtsRequestPendingScreen({navigation}) {
  return (
      <RequestFlirts navigation={navigation}/>
  );
}
