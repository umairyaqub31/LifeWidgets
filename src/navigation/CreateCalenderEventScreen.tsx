import * as React from "react";
import { CreateCalenderEvent } from "@containers";

export default function CreateCalenderEventScreen({ navigation, route }) {
  return <CreateCalenderEvent navigation={navigation} route={route} />;
}
