import * as React from 'react';
import {ChoosePrivacyPost} from '@containers';

export default function ChoosePrivacyPostScreen({navigation}) {
  return (
      <ChoosePrivacyPost navigation={navigation} />
  );
}
