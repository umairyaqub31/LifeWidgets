import * as React from "react";
import { CreateEvent } from "@containers";

export default function CreateEventScreen({ navigation, route }) {
  return <CreateEvent navigation={navigation} route={route} />;
}
