import * as React from 'react';
import { PendingEvents } from '@containers';


export default function PendingEventsScreen({navigation}) {
  return (
      <PendingEvents navigation={navigation}/>
  );
}
