import * as React from 'react';
import { GroupAdd } from '@containers';
export default function GroupAddScreen({navigation,route}) {
    
  return (
      <GroupAdd navigation={navigation} route={route}/>
  );
}
