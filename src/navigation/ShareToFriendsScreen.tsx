import * as React from 'react';
import { StyleSheet, Image, TouchableOpacity, Button } from 'react-native';
import {ShareToFriends} from '@containers';

export default function ShareToFriendsScreen({navigation}) {
  return (
      <ShareToFriends navigation={navigation} />
  );
}
