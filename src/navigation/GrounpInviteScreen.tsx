import * as React from 'react';
import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import {GroupInvite} from '@containers';


export default function GrounpInviteScreen({navigation}) {
  return (
      <GroupInvite navigation={navigation}/>
  );
}
