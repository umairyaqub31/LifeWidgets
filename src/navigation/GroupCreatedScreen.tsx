import * as React from 'react';
import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import {GroupCreated} from '@containers';


export default function GroupCreatedScreen({navigation}) {
  return (
      <GroupCreated navigation={navigation}/>
  );
}
