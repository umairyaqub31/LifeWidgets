import * as React from 'react';
import {GroupInviteFriend} from '@containers';

export default function GroupInviteFriendScreen({route,navigation}) {
  return (
      <GroupInviteFriend navigation={navigation} route={route} />
  );
}
