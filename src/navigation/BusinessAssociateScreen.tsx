import * as React from 'react';
import { BusinessAssociate } from '@containers';


export default function BusinessAssociateScreen({navigation}) {
  return (
      <BusinessAssociate navigation={navigation}/>
  );
}
