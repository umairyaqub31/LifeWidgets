import * as React from 'react';
import { StyleSheet, Image, TouchableOpacity, Button } from 'react-native';
import {ShareInGroup} from '@containers';

export default function ShareInGroupScreen({navigation}) {
  return (
      <ShareInGroup navigation={navigation} />
  );
}
