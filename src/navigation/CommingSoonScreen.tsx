import * as React from 'react';
import { CommingSoon } from '@containers';


export default function CommingSoonScreen({navigation, route}) {
  return (
      <CommingSoon navigation={navigation} route={route}/>
  );
}
