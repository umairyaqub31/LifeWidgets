import * as React from "react";
import { InvitedContacts } from "@containers";

export default function InvitedContactsScreen({ navigation }) {
  return <InvitedContacts navigation={navigation} />;
}
